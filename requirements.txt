langchain>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-core>=0.3,<0.4
pydantic>=2.5,<3
python-dotenv>=1.0,<2

# Web layer (optional — only needed for the browser frontend / server.py)
fastapi>=0.110,<1
uvicorn>=0.29,<1
